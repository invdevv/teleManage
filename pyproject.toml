[project]
name = "telemanage"
version = "0.1.0"
description = "Tool for managing Telegram account"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.13.4",
    "typer>=0.25.1",
]
